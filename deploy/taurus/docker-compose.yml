services:
  image-this-mcp:
    build:
      context: ../..
      dockerfile: Dockerfile
    container_name: image-this-mcp
    restart: unless-stopped
    env_file:
      - .env
    network_mode: host
    volumes:
      - image_this_output:/data/output

volumes:
  image_this_output:
